name: Release Artifacts

on:
  push:
    tags: ['v*.*.*']

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2

      - run: bun install

      - name: Build
        run: bun run build

      - name: Validate
        run: bun run validate

      - name: Package release artifacts
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          mkdir -p release
          cd dist/plugin && zip -r ../../release/liminal-spec-plugin-v${VERSION}.zip . && cd ../..
          cd dist/standalone && zip -r ../../release/liminal-spec-standalone-v${VERSION}.zip *-skill.md && cd ../..
          cp dist/standalone/*-skill.md release/
          cp dist/standalone/*.skill release/

      - name: Create or update Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG=${GITHUB_REF#refs/tags/}
          gh release create "$TAG" \
            release/liminal-spec-plugin-*.zip \
            release/liminal-spec-standalone-*.zip \
            release/*-skill.md \
            release/*.skill \
            --title "$TAG" \
            --generate-notes \
            2>/dev/null \
          || gh release upload "$TAG" \
            release/liminal-spec-plugin-*.zip \
            release/liminal-spec-standalone-*.zip \
            release/*-skill.md \
            release/*.skill \
            --clobber
